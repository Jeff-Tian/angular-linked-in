html
    head
        meta(charset="utf-8")
        title Callback
    body
        p #{resultString}

        script(src="/bower_components/angular/angular.min.js")
        script.
            angular.module('angularLinkedInModule', [])
            ;

            window.opener.postMessage('#{resultString.replace(/[\r\n]/g, "")}', '*');
            window.close();